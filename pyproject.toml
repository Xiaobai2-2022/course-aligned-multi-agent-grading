[project]
name = "core"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Xiaobai2-2026", email = "z2367li@uwaterloo.ca" }
]
requires-python = ">=3.12"
dependencies = [
    "pdf2image>=1.17.0",
    "pillow>=12.3.0",
    "requests>=2.34.2",
]

[project.scripts]
core = "core:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"

[tool.uv.workspace]
members = [
    "preproc-rubric",
]
